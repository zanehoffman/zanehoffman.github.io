#!/usr/bin/env bash
set -euo pipefail

docker run --rm \
  -p 4000:4000 \
  -v "$PWD:/srv/jekyll" \
  -w /srv/jekyll \
  ruby:3.3-bookworm \
  bash -lc 'bundle config set path vendor/bundle && bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload'
